EHEAPSERVER=localhost
MACHINENAME=localhost
if [ $# -ge 1 ]
then
    EHEAPSERVER=$1;
fi
if [ $# -ge 2 ]
then 
    EHEAPSERVER=$1;
    MACHINENAME=$2;
fi
echo $EHEAPSERVER $MACHINENAME;
java -classpath ../../Lib/iROS.jar:. TerminalEventSender $EHEAPSERVER $MACHINENAME $3 $4 $5 $6 $7 $8 $9;
