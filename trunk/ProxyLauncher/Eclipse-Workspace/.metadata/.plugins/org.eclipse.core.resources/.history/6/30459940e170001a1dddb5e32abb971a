import java.awt.Color;
import java.awt.Graphics;

import javax.swing.AbstractButton;
import javax.swing.JButton;
import javax.swing.JComponent;
import javax.swing.JToggleButton;
import javax.swing.plaf.basic.BasicButtonUI;
import javax.swing.plaf.basic.BasicToggleButtonUI;
import javax.swing.plaf.metal.MetalButtonUI;

import com.sun.corba.se.spi.legacy.connection.GetEndPointInfoAgainException;
import com.sun.org.apache.bcel.internal.generic.SWITCH;

public class CustomButtonUI extends BasicToggleButtonUI{
	
	private Color selectedColor; 
	private Color deselectedColor;
	private Color trueColor;
	public CustomButtonUI (Color selectedColor, Color deselectedColor){
		System.out.println("In CustomUI. Disabled Text Color = " );
		trueColor = selectedColor;
	}

	//public void update (Graphics g, JComponent c){
		//System.out.println("In neuer update-Methode");
		//this.paint(g,c);
	//}
	
	public void paintButtonPressed (Graphics g, AbstractButton b){
		
		if (b.getBackground() == Color.RED)//deselectedColor)
			b.setBackground(Color.GREEN);
		else if (b.getBackground() == Color.GREEN)
			b.setBackground(Color.RED);//selectedColor); 
			
		
		
		//if (b.getBackground() == Color.RED)
			//b.setBackground(Color.GREEN);
		
		System.out.println("IN OVERWRITTEN PAINT METHOD TRUE-Branch");
	}
	//public void paint (Graphics g, JComponent c)
	
	//TODO Seems To be right in here
		//This method is called when the button is clicked
		//super.paint(g,c);	
		//ChangeColorButton temp = new ChangeColorButton();
		//temp = c;
	//	if (c.getBackground() == Color.GREEN)
		//	c.setBackground(Color.WHITE);
		//else
			
		//c.setBackground(Color.PINK);// = temp;
		
		//super.paint(g,c);
		//ChangeColorButton actualButton = (ChangeColorButton) c;
		//actualButton.getActivatedColor();
		/*if (b.getBackground().equals(this.selectedColor))
				
		{
					System.out.println("IN OVERWRITTEN PAINT METHOD TRUE-Branch");
					b.setBackground(Color.GREEN);//deselectedColor);
				}
		else
		{
			System.out.println("IN OVERWRITTEN PAINT METHOD FALSE-Branch");
			b.setBackground(selectedColor);
		}*/
		//c.getClass().
		//System.out.println("IN OVERWRITTEN PAINT METHOD " + c.getClass());//+ b.getText());// +b.getActivatedColor() );
		
}
