import java.io.File;

import javax.swing.filechooser.FileFilter;

public class CustomFileFilter extends FileFilter {
	
	private String _filterType;
	private String _description;
	public CustomFileFilter (String behavior) {
		_filterType = behavior;
	}
	 
	public boolean accept (File f) {
		/*if ((_filterType.equals("iros")) || (_filterType.equals("jmdns")) ) {
			_description = "Jar Files";
			if (f.getName().contains(".jar"));
				return (true);
		}
		else if (_filterType.equals("directory")) {
			_description = "Proxy Directories";
			if (f.isDirectory())
				return (true);	
		}
		 */
		return(false); // In any other case.
	 }
	
	 public String getDescription () {
		 return (_description);
	 }	
	
}
